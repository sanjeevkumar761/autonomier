apiVersion: v1
kind: Service
metadata:
  name: {{ include "one-touch-sap-deployment.fullname" . }}
  labels:
    {{- include "one-touch-sap-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "one-touch-sap-deployment.selectorLabels" . | nindent 4 }}
