apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "one-touch-sap-deployment.fullname" . }}-test-connection"
  labels:
    {{- include "one-touch-sap-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "one-touch-sap-deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
